pipeline {
    agent any 
    environment{
    DOCKERHUB_CREDENTIALS = credentials('vaishnd-dockerhub')
    }
    stages {
        stage ("Build") {
            steps{     
	       sh './build.sh'
            }
        }
        stage ("deploy") {
            steps{
                sh './deploy.sh' 
            }
        }
    }
}
